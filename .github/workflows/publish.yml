name: Publish to PyPI

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install build
      - run: python -m build
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  publish-pypi:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/tz-overlap
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      # Try OIDC first (trusted publisher), fall back to token auth
      - uses: pypa/gh-action-pypi-publish@release/v1
        if: ${{ !secrets.PYPI_TOKEN }}
      - uses: pypa/gh-action-pypi-publish@release/v1
        if: ${{ secrets.PYPI_TOKEN }}
        with:
          password: ${{ secrets.PYPI_TOKEN }}

  publish-testpypi:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: testpypi
      url: https://test.pypi.org/p/tz-overlap
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      # Try OIDC first (trusted publisher), fall back to token auth
      - uses: pypa/gh-action-pypi-publish@release/v1
        if: ${{ !secrets.PYPI_TOKEN }}
        with:
          repository-url: https://test.pypi.org/legacy/
      - uses: pypa/gh-action-pypi-publish@release/v1
        if: ${{ secrets.PYPI_TOKEN }}
        with:
          repository-url: https://test.pypi.org/legacy/
          password: ${{ secrets.PYPI_TOKEN }}
